// =================================================================================================
// Dispatch
// =================================================================================================

// Handle

pub trait Handle<T> {
    fn handle(&self, command: T);
}

pub trait HandleMut<T> {
    fn handle_mut(&mut self, command: T);
}
